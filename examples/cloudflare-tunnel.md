# Cloudflare Tunnel

Install `cloudflared` from Cloudflare's current repo instructions or a
[verified binary download](binaries.md).

Store tunnel credentials inside the VM. Do not put tokens in DVM config.
